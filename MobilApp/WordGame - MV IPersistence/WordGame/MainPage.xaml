<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WordGame.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Frame BackgroundColor="LightSkyBlue">
                <Label
                    Text="Word Game!"
                    SemanticProperties.HeadingLevel="Level1"
                    FontSize="32"
                    HorizontalOptions="Center" />
            </Frame>

            <Label
                x:Name="_currentWordLabel"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Current Word: " />
                        <Span Text="{Binding CurrentWord}" FontAttributes="Bold"/>
                    </FormattedString>
                </Label.FormattedText>
            </Label>

            <Label
                x:Name="_scoreLabel" Text="{Binding CurrentScore}"
                SemanticProperties.HeadingLevel="Level2"
                FontSize="14"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <Entry x:Name="_wordEntry" Placeholder="Enter your word here" HorizontalOptions="FillAndExpand" Text="{Binding UserWord, Mode=TwoWay}" ReturnCommand="{Binding AcceptWordCommand}"/>

            <Button x:Name="_acceptButton" Text="Accept"
                    Command="{Binding AcceptWordCommand}"/>

            <Button x:Name="_newGameButton" Text="New Game" Margin="0,16,0,0"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

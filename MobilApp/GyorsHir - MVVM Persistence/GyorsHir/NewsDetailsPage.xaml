<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GyorsHir.NewsDetailsPage"
             Title="News Details"
             BindingContext="{Binding SelectedNewsItem}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackLayout>
            <Label Text="{Binding Title}" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" FontAttributes="Bold" FontSize="Large" />
            <Label HorizontalTextAlignment="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Author: " FontAttributes="Bold" />
                        <Span Text="{Binding Author}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <Button Text="Share" Command="{Binding ShareCommand}" CommandParameter="{Binding}" />
        </StackLayout>

        <ScrollView Grid.Row="1" Margin="4">
            <StackLayout>
                <Label Text="{Binding Description}" FontSize="Body"/>
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GyorsHir.MainPage"
             Title="Gyors Hír!">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Frame BackgroundColor="#2196F3" Padding="24,6" CornerRadius="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Text="Gyors Hír!" Grid.ColumnSpan="2" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
                <Entry Grid.Row="1" Text="{Binding SearchFilter, Mode=TwoWay}" ReturnCommand="{Binding SearchCommand}"></Entry>
                <Button Text="Search" Grid.Row="1" Grid.Column="1" Command="{Binding SearchCommand}"></Button>
            </Grid>
        </Frame>

        <ListView Grid.Row="1" ItemsSource="{Binding NewsPortals}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}" Detail="{Binding ShortDescription}"
                          Command="{Binding SelectCommand}" CommandParameter="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</ContentPage>

const temp = [
    3.4, 1.2, -0.7, -2, 20, 6, 12
];

//const matrix = Math.matrix([[7, 1], [-2, 3]])

// const freezedDays = []

// temp.filter(temp => temp <0).map(temp => freezedDays.push(temp));
// const freezedDays2 = temp.filter(temp => temp <0);

// console.log(freezedDays2);

// console.log(freezedDays);

// console.log(`task2:`);

// //temp.forEach((element, i ,temp) => {
// //    temp[i] += ' C';
// //});

// console.log(temp);

// const newArray = [];

// temp.map((element) => {
//     newArray.push(element + ' C');
// });

// console.log(newArray);

// const maxTemp = Math.max(...temp);
// const minTemp = Math.min(...temp);

// console.log(`Max temperature: ${maxTemp}`);
// console.log(`Min temperature: ${minTemp}`);

// const max2 = temp.reduce((m, x) => m > x ? m : x); 
// const min2 = temp.reduce((m, x) => m < x ? m : x)

// console.log(`Max2 temperature: ${max2}`);
// console.log(`Min2 temperature: ${min2}`);

// const vane = temp.some(t => t <= 0) ? 'Van' : 'Nincs';
// console.log(`Van-e fagyos nap? ${vane}`);

// const vane2 = temp.every(t => t > 0) ? 'Nincs' : 'Van';
// console.log(`Van-e fagyos nap? ${vane2}`);

//const underTwenty = temp.filter(t => t <20);
//console.log(underTwenty.length);

//console.log(temp.some(t => t> 40) ? 'van' : 'nincs');

//console.log(temp.find(t => t > 10));

// 16. feladat

//const everyItemOdd = matrix.map(x => x % 2 !== 0);
//console.log(everyItemOdd);

